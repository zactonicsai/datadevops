# NiFi — the visual conveyor-belt builder that moves Kafka messages to S3.
apiVersion: v1
kind: Service
metadata:
  name: nifi
  namespace: ${namespace}
spec:
  selector:
    app: nifi
  ports:
    - name: https
      port: 8443
      targetPort: 8443
