version: "3.8"

# =============================================================================
# Grocery Operations Intelligence Platform
# Single-file orchestration: 3x Kafka (KRaft mode, no ZooKeeper), one ZooKeeper
# dedicated to NiFi, NGINX load balancer, message-api (scaled x3), ingest
# worker, analytics, dashboard, Postgres, MinIO (binary store), ChromaDB
# (vectors) and Ollama (local LLM).
# =============================================================================

x-kafka-common: &kafka-common
  image: confluentinc/cp-kafka:7.6.1
  restart: unless-stopped
  # KRaft cluster id — shared by all brokers. Override by exporting CLUSTER_ID
  # (must be a 22-char base64 UUID, e.g. from `kafka-storage random-uuid`).
  healthcheck:
    test: ["CMD", "bash", "-c", "kafka-broker-api-versions --bootstrap-server localhost:29092 >/dev/null 2>&1"]
    interval: 15s
    timeout: 10s
    retries: 10
    start_period: 40s

services:
  # ---------------------------------------------------------------------------
  # ZooKeeper — dedicated to NiFi only (Kafka now runs in KRaft mode and needs
  # no ZooKeeper). A single node is fine for a standalone NiFi's state
  # management and avoids multi-node quorum complexity.
  # ---------------------------------------------------------------------------
  zookeeper-nifi:
    image: confluentinc/cp-zookeeper:7.6.1
    container_name: gsp-zookeeper-nifi
    restart: unless-stopped
    environment:
      ZOOKEEPER_SERVER_ID: 1
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
      # allow the 'ruok' four-letter word so the healthcheck below can probe it
      ZOOKEEPER_4LW_COMMANDS_WHITELIST: "ruok,srvr,stat"
    healthcheck:
      test: ["CMD-SHELL", "echo ruok | nc -w 2 localhost 2181 | grep -q imok"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    volumes:
      - zk-nifi-data:/var/lib/zookeeper/data
      - zk-nifi-log:/var/lib/zookeeper/log
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Kafka cluster (3 brokers, KRaft mode — combined broker+controller)
  # ---------------------------------------------------------------------------
  kafka1:
    <<: *kafka-common
    container_name: gsp-kafka1
    environment:
      # --- KRaft: this node is both broker and controller ---
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: "broker,controller"
      CLUSTER_ID: "${CLUSTER_ID:-OFXVWG8FR8anxcz23e7kMw}"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka1:29093,2@kafka2:29093,3@kafka3:29093"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      # --- listeners: CONTROLLER (KRaft) + INTERNAL (inter-broker) + EXTERNAL (host) ---
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT"
      KAFKA_LISTENERS: "CONTROLLER://0.0.0.0:29093,INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9092"
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka1:29092,EXTERNAL://localhost:9092"
      KAFKA_INTER_BROKER_LISTENER_NAME: "INTERNAL"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 2
      KAFKA_DEFAULT_REPLICATION_FACTOR: 3
      KAFKA_MIN_INSYNC_REPLICAS: 2
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 3
    ports: ["9092:9092"]
    volumes: [kafka1-data:/var/lib/kafka/data]
    networks: [grocery-net]

  kafka2:
    <<: *kafka-common
    container_name: gsp-kafka2
    environment:
      KAFKA_NODE_ID: 2
      KAFKA_PROCESS_ROLES: "broker,controller"
      CLUSTER_ID: "${CLUSTER_ID:-OFXVWG8FR8anxcz23e7kMw}"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka1:29093,2@kafka2:29093,3@kafka3:29093"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT"
      KAFKA_LISTENERS: "CONTROLLER://0.0.0.0:29093,INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9093"
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka2:29092,EXTERNAL://localhost:9093"
      KAFKA_INTER_BROKER_LISTENER_NAME: "INTERNAL"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 2
      KAFKA_DEFAULT_REPLICATION_FACTOR: 3
      KAFKA_MIN_INSYNC_REPLICAS: 2
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 3
    ports: ["9093:9093"]
    volumes: [kafka2-data:/var/lib/kafka/data]
    networks: [grocery-net]

  kafka3:
    <<: *kafka-common
    container_name: gsp-kafka3
    environment:
      KAFKA_NODE_ID: 3
      KAFKA_PROCESS_ROLES: "broker,controller"
      CLUSTER_ID: "${CLUSTER_ID:-OFXVWG8FR8anxcz23e7kMw}"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka1:29093,2@kafka2:29093,3@kafka3:29093"
      KAFKA_CONTROLLER_LISTENER_NAMES: "CONTROLLER"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT"
      KAFKA_LISTENERS: "CONTROLLER://0.0.0.0:29093,INTERNAL://0.0.0.0:29092,EXTERNAL://0.0.0.0:9094"
      KAFKA_ADVERTISED_LISTENERS: "INTERNAL://kafka3:29092,EXTERNAL://localhost:9094"
      KAFKA_INTER_BROKER_LISTENER_NAME: "INTERNAL"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 2
      KAFKA_DEFAULT_REPLICATION_FACTOR: 3
      KAFKA_MIN_INSYNC_REPLICAS: 2
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 3
    ports: ["9094:9094"]
    volumes: [kafka3-data:/var/lib/kafka/data]
    networks: [grocery-net]

  # One-shot: create all grocery topics once the cluster is healthy
  kafka-setup:
    image: confluentinc/cp-kafka:7.6.1
    container_name: gsp-kafka-setup
    depends_on:
      kafka1: {condition: service_healthy}
      kafka2: {condition: service_healthy}
      kafka3: {condition: service_healthy}
    entrypoint: ["/bin/bash", "/setup/create-topics.sh"]
    volumes:
      - ./kafka-config:/setup:ro
    networks: [grocery-net]
    restart: "no"

  # ---------------------------------------------------------------------------
  # PostgreSQL — system of record
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: gsp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: grocery
      POSTGRES_PASSWORD: grocery_pw
      POSTGRES_DB: grocery
    volumes:
      - pg-data:/var/lib/postgresql/data
      - ./init/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U grocery"]
      interval: 10s
      timeout: 5s
      retries: 10
    ports: ["5432:5432"]
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # MinIO — S3-compatible object store for uploaded binary files
  # ---------------------------------------------------------------------------
  minio:
    image: minio/minio:RELEASE.2024-06-13T22-53-53Z
    container_name: gsp-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes: [minio-data:/data]
    ports: ["9000:9000", "9001:9001"]
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 15s
      timeout: 10s
      retries: 5
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # ChromaDB — vector store for what-if retrieval context
  # ---------------------------------------------------------------------------
  chromadb:
    image: chromadb/chroma:0.5.5
    container_name: gsp-chromadb
    restart: unless-stopped
    environment:
      IS_PERSISTENT: "TRUE"
      ANONYMIZED_TELEMETRY: "FALSE"
    volumes: [chroma-data:/chroma/chroma]
    ports: ["8000:8000"]
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Ollama — local LLM for scenario reasoning
  # ---------------------------------------------------------------------------
  ollama:
    image: ollama/ollama:latest
    container_name: gsp-ollama
    restart: unless-stopped
    volumes: [ollama-data:/root/.ollama]
    ports: ["11434:11434"]
    networks: [grocery-net]

  # Pulls the model once on startup (llama3.2 — small, runs on CPU)
  ollama-pull:
    image: ollama/ollama:latest
    container_name: gsp-ollama-pull
    depends_on: [ollama]
    # Pull the model, then run a tiny warmup generation so it's loaded into
    # memory (keep_alive 30m). Pulling alone doesn't load it; the first real
    # what-if would otherwise pay the cold-load cost and risk a timeout.
    entrypoint: ["/bin/sh", "-c", "sleep 8 && export OLLAMA_HOST=http://ollama:11434 && ollama pull llama3.2 && echo 'warming up model...' && ollama run llama3.2 --keepalive 30m 'ok' >/dev/null 2>&1; echo done"]
    networks: [grocery-net]
    restart: "no"

  # ---------------------------------------------------------------------------
  # Auto-seed — loads ~90 days of demo data + scenarios once Postgres is healthy
  # so the dashboard has data on first load (no manual step). Idempotent: the
  # script skips if the DB is already seeded. Set SEED_FORCE=1 to force a reseed.
  # ---------------------------------------------------------------------------
  seed:
    image: python:3.12-slim
    container_name: gsp-seed
    depends_on:
      postgres: {condition: service_healthy}
    environment:
      DATABASE_URL: "postgresql://grocery:grocery_pw@postgres:5432/grocery"
      SEED_FORCE: "0"
    volumes:
      - ./scripts:/scripts:ro
    command: ["/bin/sh", "-c", "pip install --no-cache-dir psycopg2-binary==2.9.9 >/dev/null && python /scripts/seed_data.py"]
    restart: "no"
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Apache NiFi — visual dataflow, using the SAME Zookeeper ensemble for state
  # management (nifi.state.management.embedded.zookeeper.start=false). Runs
  # unsecured over HTTP for local use: the HTTPS_* vars and keystore must be
  # blanked or the image defaults back to HTTPS and demands a keystore.
  # UI reachable via the load balancer at http://localhost:8080/nifi/
  # ---------------------------------------------------------------------------
  nifi:
    image: apache/nifi:2.6.0
    container_name: gsp-nifi
    restart: unless-stopped
    depends_on:
      zookeeper-nifi: {condition: service_healthy}
    environment:
      # --- run unsecured on HTTP (no TLS/keystore) ---
      NIFI_WEB_HTTP_HOST: "0.0.0.0"
      NIFI_WEB_HTTP_PORT: "8080"
      NIFI_WEB_HTTPS_HOST: ""
      NIFI_WEB_HTTPS_PORT: ""
      NIFI_SECURITY_KEYSTORE: ""
      NIFI_SECURITY_KEYSTOREPASSWD: ""
      NIFI_SECURITY_KEYPASSWD: ""
      NIFI_SECURITY_TRUSTSTORE: ""
      NIFI_SECURITY_TRUSTSTOREPASSWD: ""
      NIFI_REMOTE_INPUT_SECURE: "false"
      # served behind the LB under the /nifi/ path
      NIFI_WEB_PROXY_CONTEXT_PATH: "/nifi"
      NIFI_WEB_PROXY_HOST: "localhost:8080,localhost"
      # --- use the dedicated ZooKeeper for cluster state (not embedded) ---
      NIFI_ZK_CONNECT_STRING: "zookeeper-nifi:2181"
      NIFI_ZK_ROOT_NODE: "/nifi"
      NIFI_ELECTION_MAX_WAIT: "30 sec"
      NIFI_ELECTION_MAX_CANDIDATES: "1"
      NIFI_JVM_HEAP_INIT: "1g"
      NIFI_JVM_HEAP_MAX: "2g"
    volumes:
      - nifi-conf:/opt/nifi/nifi-current/conf
      - nifi-state:/opt/nifi/nifi-current/state
      - nifi-content:/opt/nifi/nifi-current/content_repository
      - nifi-flowfile:/opt/nifi/nifi-current/flowfile_repository
      - nifi-provenance:/opt/nifi/nifi-current/provenance_repository
      - nifi-database:/opt/nifi/nifi-current/database_repository
    expose: ["8080"]
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Message API — text / json / binary -> Kafka  (scaled to 3 replicas)
  # ---------------------------------------------------------------------------
  message-api:
    build: ./services/message-api
    restart: unless-stopped
    depends_on:
      kafka1: {condition: service_healthy}
      minio: {condition: service_started}
    environment:
      KAFKA_BOOTSTRAP: "kafka1:29092,kafka2:29092,kafka3:29092"
      MINIO_ENDPOINT: "minio:9000"
      MINIO_ACCESS_KEY: "minioadmin"
      MINIO_SECRET_KEY: "minioadmin"
      MINIO_BUCKET: "grocery-uploads"
    expose: ["8001"]
    deploy:
      replicas: 3
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Ingest worker — Kafka -> Postgres
  # ---------------------------------------------------------------------------
  ingest-worker:
    build: ./services/ingest-worker
    restart: unless-stopped
    depends_on:
      kafka1: {condition: service_healthy}
      postgres: {condition: service_healthy}
    environment:
      KAFKA_BOOTSTRAP: "kafka1:29092,kafka2:29092,kafka3:29092"
      DATABASE_URL: "postgresql://grocery:grocery_pw@postgres:5432/grocery"
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Analytics — pandas / scikit-learn metrics, forecasts, projections, what-if
  # ---------------------------------------------------------------------------
  analytics:
    build: ./services/analytics
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      seed: {condition: service_completed_successfully}
      chromadb: {condition: service_started}
      ollama: {condition: service_started}
    environment:
      DATABASE_URL: "postgresql://grocery:grocery_pw@postgres:5432/grocery"
      CHROMA_HOST: "chromadb"
      CHROMA_PORT: "8000"
      OLLAMA_URL: "http://ollama:11434"
      OLLAMA_MODEL: "llama3.2"
    expose: ["8002"]
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # Dashboard — static HTML + Tailwind + SVG charts
  # ---------------------------------------------------------------------------
  dashboard:
    build: ./services/dashboard
    restart: unless-stopped
    depends_on: [analytics]
    environment:
      ANALYTICS_URL: "http://analytics:8002"
    expose: ["8003"]
    networks: [grocery-net]

  # ---------------------------------------------------------------------------
  # NGINX — load balancer / single entry point
  # ---------------------------------------------------------------------------
  lb:
    image: nginx:1.27-alpine
    container_name: gsp-lb
    restart: unless-stopped
    depends_on: [message-api, dashboard, analytics, nifi]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports: ["8080:80"]
    networks: [grocery-net]

networks:
  grocery-net:
    driver: bridge

volumes:
  zk-nifi-data: {}
  zk-nifi-log: {}
  kafka1-data: {}
  kafka2-data: {}
  kafka3-data: {}
  pg-data: {}
  minio-data: {}
  chroma-data: {}
  ollama-data: {}
  nifi-conf: {}
  nifi-state: {}
  nifi-content: {}
  nifi-flowfile: {}
  nifi-provenance: {}
  nifi-database: {}
